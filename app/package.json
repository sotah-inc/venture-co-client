{
  "name": "@sotah-inc/venture-co-client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@sotah-inc/client": "^1.0.19",
    "@types/jest": "24.0.16",
    "@types/node": "12.6.9",
    "@types/react": "16.8.24",
    "@types/react-dom": "16.8.5",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "serve": "^11.1.0",
    "typescript": "3.5.3"
  },
  "scripts": {
    "start": "serve -s -l tcp://localhost:$PORT -c ./serve.json",
    "start-dev": "serve -s -l tcp://0.0.0.0:$PORT -c ./serve.json",
    "debug": "react-scripts start",
    "gcp-build": "npm run -s build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
